# API bind/auth
API_BIND_ADDR=0.0.0.0:8080
# Set to a strong random value (32+ chars) to require authentication on
# /api/* routes.  Leave empty to allow unauthenticated access.
API_KEY=

# Logging/runtime
RUST_LOG=info
# Set to 1 for local dev (fake proofs); must be 0 in production.
RISC0_DEV_MODE=0

# Storage (used by supervisord deploy; defaults to ./data if unset)
DATA_DIR=/var/lib/stellar-zk/prover

# Request + queueing limits
MAX_TAPE_BYTES=2097152
MAX_JOBS=64
JOB_TTL_SECS=86400
JOB_SWEEP_SECS=60

# Prover policy
MAX_FRAMES=18000
MIN_SEGMENT_LIMIT_PO2=16
MAX_SEGMENT_LIMIT_PO2=21
RUNNING_JOB_TIMEOUT_SECS=600
# After a timeout, abort the proof task after this grace period.
# Set to 0 to disable forced abort (not recommended in production).
TIMED_OUT_PROOF_KILL_SECS=60

# HTTP server tuning
HTTP_MAX_CONNECTIONS=25000
HTTP_KEEP_ALIVE_SECS=75
# HTTP_WORKERS=4

# Optional browser origin for CORS; keep unset for server-to-server only.
# CORS_ALLOWED_ORIGIN=https://yourapp.example.com

# Cloudflare Tunnel token (used by cloudflared supervisor config).
# Get the token from Cloudflare Zero Trust > Networks > Tunnels > <tunnel> > Configure.
CLOUDFLARE_TUNNEL_TOKEN=
