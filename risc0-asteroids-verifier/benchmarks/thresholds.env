# Coverage quality gates (core crate).
COVERAGE_MIN_REGIONS=85.0
COVERAGE_MIN_LINES=85.0
COVERAGE_MIN_FUNCTIONS=88.0

# Dev mode timing gates (seconds).
DEV_SHORT_MAX_REAL_S=2.0
DEV_MEDIUM_MAX_REAL_S=2.0

# Secure mode timing gates (seconds).
SECURE_SHORT_MAX_REAL_S=60.0
SECURE_MEDIUM_MAX_REAL_S=600.0

# Secure mode memory gates (bytes, /usr/bin/time -l max RSS).
SECURE_SHORT_MAX_RSS_BYTES=7000000000
SECURE_MEDIUM_MAX_RSS_BYTES=13000000000

# Cycle-count gates from RISC0_INFO logs.
DEV_SHORT_MAX_TOTAL_CYCLES=700000
DEV_MEDIUM_MAX_TOTAL_CYCLES=7000000
