# Core deterministic cycle gates for execute-only benchmark binary.
#
# Command:
#   bash scripts/bench-core-cycles.sh --threshold-mode check
#
# Values should have modest headroom above expected baseline to catch
# regressions while tolerating minor toolchain drift.

SHORT_MAX_TOTAL_CYCLES=470000
MEDIUM_MAX_TOTAL_CYCLES=6300000
REAL_MAX_TOTAL_CYCLES=29500000
